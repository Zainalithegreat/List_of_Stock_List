from WebUI import WebUI
from UserState import  UserState

if __name__ == '__main__':
    from Database import Database
    #WebUI.init()
    WebUI.run()
    #user = Database.read_user("Zain")
    #user_state = UserState(user)
